pipeline {
    agent any

    stages {
        stage('Checkout Verification') {
            steps {
                echo 'Enterprise Data Platform CI/CD pipeline started'
                sh 'git status'
            }
        }
    }
}
